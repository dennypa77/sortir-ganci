# Core dependencies untuk Robot Sortir Desain & Stasiun Sortir
# Install: pip install -r requirements.txt

pandas>=2.0
openpyxl>=3.1

# Google Sheets sync (opsional — aplikasi tetap jalan tanpa ini, tapi
# fitur sinkronisasi cloud akan di-skip otomatis)
gspread>=6.0
google-auth>=2.20

# UI modern untuk stasiun_sortir.py (scan-to-sort)
customtkinter>=5.2

# === Optional dev tools ===
# edge-tts + imageio-ffmpeg hanya dipakai oleh `generate_audio.py` (sekali
# untuk membuat audio/slot_NN.mp3 + slot_NN.wav). Runtime stasiun_sortir
# TIDAK butuh keduanya — playback pakai winsound built-in (untuk WAV) atau
# Windows MCI fallback (untuk MP3). Uncomment kalau mau regenerate audio:
# edge-tts>=6.1
# imageio-ffmpeg>=0.5
